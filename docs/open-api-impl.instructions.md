Act as a Principal Python Backend Engineer. I need you to generate a production-ready, modular FastAPI project structure based on an existing OpenAPI specification located at `/docs/open-api-spec.json`.

Strictly adhere to a clean architecture pattern that decouples the HTTP transport layer (FastAPI Routers) from the business logic layer (Service Layer) using Dependency Injection.

### Project Initialization & Package Management
Use **uv** as the Python package manager. Provide the commands and configurations to:
1. Initialize the project environment using `uv init` if not initialized.
2. Manage dependencies (including `fastapi`, `uvicorn`, and `pydantic`) using `uv add`.
3. Provide a standard `pyproject.toml` generated by `uv`.

### Project Directory Structure to Follow:
```text
my_fastapi_project/
├── pyproject.toml              # Managed via uv
├── uv.lock                     # Managed via uv
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI app initialization & global middlewares/handlers
│   ├── api/
│   │   ├── __init__.py
│   │   ├── deps.py             # Dependency injection providers for services
│   │   └── v1/                 # Versioned route endpoints
│   ├── schemas/                # Pydantic models (request/response validation)
│   └── services/               # Pure business logic interfaces (with placeholders)
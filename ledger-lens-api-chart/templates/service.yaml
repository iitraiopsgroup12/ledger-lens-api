apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-lens-api.fullname" . }}
  labels:
    {{- include "ledger-lens-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ledger-lens-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
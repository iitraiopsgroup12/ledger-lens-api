apiVersion: v2
name: ledger-lens-api
description: LedgerLens.ai Platform API — FastAPI service for monitoring NSE-listed companies and RAG-powered chat over filings.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — defaults the image tag (overridable via image.tag).
appVersion: "1.0.0"